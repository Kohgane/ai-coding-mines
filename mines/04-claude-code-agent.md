# Claude Code · AI 에이전트

에이전트에게 일을 맡길 때 생기는 함정. 대부분 **에이전트가 "했다"고 보고한 것과 실제로 남은 것이 다른** 형태다.

---

## 훅 command의 `%USERPROFILE%`는 확장되지 않는다

**증상**
훅을 등록했는데 세션 시작 시 조용한 startup hook error. 훅이 아예 안 돈다.

**원인**
훅 `command` 문자열은 셸 환경변수 확장을 거치지 않는다. `%USERPROFILE%\...` 가 리터럴 문자열 그대로 경로로 쓰인다.

**해법**
설치 시점에 **실제 전체 경로 리터럴**을 박는다.

같은 이유로 스케줄러(schtasks) 등록에도 환경변수가 아닌 절대경로를 쓴다. 한글이 들어간 경로라면 인코딩 문제를 먼저 해결한다.

---

## 웹 세션 컨테이너는 일회용이다

**증상**
웹 세션에서 홈 디렉터리에 만든 파일·설정이 전부 증발한다.

**원인**
브라우저에서 여는 세션은 일회용 클라우드 컨테이너다. 세션이 갈리면 확정 소멸이다.

**해법**
로컬 설치·로컬 설정은 **PC 터미널에서** 한다. 일부 UI 기능(스피너 등)도 웹/데스크톱에서는 표시 자체가 안 되므로 확인은 터미널 CLI로 한다.

---

## 세션 컨테이너가 레포를 옛 커밋으로 되돌린다

**증상**

```
HEAD        : d45e9008 (예전 커밋)
origin/main : 7f08c3ba
뒤처짐 48커밋
```

한 스레드에서 **4회 재발**했다.

**왜 위험한가**
- 모르고 작업하면 **이미 머지된 수리 위에 옛 코드를 얹는다**(수리 되돌리기)
- **전체 테스트 숫자가 무효**가 된다 — 그린이 떠도 옛 코드의 그린이다
- **조용하다.** 에러가 안 난다

**감지법**
- **사람 눈:** *최근 머지한 파일·심볼이 없다.* 있어야 할 import가 사라졌거나(실측: `import re` 소실), 테스트 수가 급감했거나, 방금 만든 함수가 없다. 이러면 **즉시 `git log --oneline -1`**
- **자동:** SessionStart 훅이 매 세션 첫 줄에 `HEAD == origin/main` 여부를 보고하게 한다. 훅이 없는 환경을 대비해 프로젝트 지침 문서의 **세션 시작 1단계**로도 고정한다

**규칙**
- 어떤 작업이든 착수 전 `git fetch origin main` → HEAD 일치 확인. 불일치면 재동기화 후 착수
- **훅은 감지·보고만. 자동 reset 금지** — 미커밋 작업이 날아간다
- 전체 테스트는 **재동기화 이후 HEAD 고정 상태**에서 돌린 것만 보고한다

**실전 검거**
훅이 4회째를 잡았다. 미커밋 파일을 스크래치패드로 피신시키고 재동기화 후 복원했다. **자동 reset을 안 넣어둔 판단이 옳았다.**

---

## 컨테이너 출력 디렉터리는 저장소가 아니다

**증상**
세션 산출물 디렉터리가 **완전히 비어 있었다**(파일 0). 빌드 산출물과 서명 키 사본이 함께 사라졌다.

**교훈 2개**
1. **산출물은 생성 즉시 로컬로 내린다.** 컨테이너는 캐시지 저장소가 아니다
2. ★ **"컨테이너에 없다"에서 "백업도 없을 것"으로 넘어간 건 추측이었고 틀렸다.** 로컬 백업이 살렸다. **실측 범위를 넘는 결론을 내지 말 것**

---

## 채팅 첨부는 도달을 보장하지 않는다

**증상**
에이전트가 채팅 첨부로 보낸 파일이 사람에게 **도달하지 않는다.** 두 번 반복됐다(확장 zip, 디자인 목업 HTML).

**규칙 (굳힘)**
- **모든 산출물 = 레포 커밋 경로 + PR 링크.** 채팅 첨부는 보조이지 신뢰 대상이 아니다
- 바이너리(zip·이미지 등 레포에 안 넣는 것) = **Releases**(태그) 또는 아티팩트 → URL로 전달
- **보고 필수 문구:** "열람 경로: PR#\<n\> · \<레포 파일경로\>". 없으면 미전달로 간주한다

---

## 에이전트 토큰은 브랜치 push는 되나 태그는 403

**증상**
에이전트의 git 프록시 토큰으로 브랜치는 push되는데 **태그 생성만 403**(curl 22). 태그 트리거 워크플로(`on: push tags`)를 발동할 수 없다.

**해법**
워크플로에 **`workflow_dispatch`(version 입력)** 를 추가하고, 그 안에서 `gh release create <ver> --target <sha>`로 해당 커밋에 태그+릴리즈를 만든다. 에이전트는 워크플로 실행 트리거만 치고, 사람은 Actions UI 버튼으로 같은 걸 누른다.

★ **태그 push가 필요한 자동화는 이 우회를 전제로 설계한다.**

---

## CI 그린은 실동작을 보증하지 않는다

**실증 3건**
- CI가 `--collect-only`만 돌려 **실패 23건이 그린으로 위장**됐다 → 실행 게이트를 환경변수로 봉인
- 스위트는 그린인데 라이브가 전면 저속 — 동기 처리가 워커 슬롯을 점유하고 있었다
- 그린인데 화면에 옛 판정값이 잔존 — 저장 스테일값을 화면이 그대로 믿었다

**해법**
그린은 **필요조건**이다. 실기기·실브라우저와 계측 지표로 라이브를 별도 검증한다. 판정은 **조회 시 재계산**한다.

**역방향 지뢰 — 회귀 계약이 '버그값'을 동결한다**
캡처된 산출물을 baseline으로 삼아 재실행 결과와 대조하는 계약 테스트에서, **캡처 시점이 버그 상태였다면 그 버그가 동결된다.** 실측: baseline이 `images: 1`(갤러리 버그)을 기록 → 수리로 1→11이 되자 계약이 red.

의도된 수리는 **baseline을 재추출값으로 갱신**해야 한다.
1. 영향 필드만 재생성
2. 나머지 계약 필드 불변 실증
3. 커밋 메시지에 **"버그 동결 아님, 교정값 반영"** 명시

**지연 import의 함정**
`try: from PIL import Image / except: return None` 은 CI collect-only 안전을 위한 정본 패턴인데, **없을 때 예외 대신 조용히 무력화**된다. 지연 import로 감싼 서드파티는 **반드시 requirements 확인 + 명시 계약**을 건다. "테스트가 통과하니 있다"는 근거가 아니다 — 그 테스트도 같은 컨테이너에서 돈다.

---

## 대조군 없이 세운 가설은 예외 없이 틀렸다

**사례 A** — 배포 후 새 라우트만 404. 세운 가설 5개(키스토어·소스맵·manifest·인프라 장애·커밋 유실)가 **전부 관측 범위를 넘어선 단정**이었고 전부 틀렸다. 진짜 원인은 의존성 목록 한 줄이었다.

**사례 B** — 세 개 배포본 중 둘만 503. "번들이 다르다"는 가설로 며칠을 썼다. 3자 대조를 해보니 **배포 관점에서 세 번들은 동일**했고, 유일한 차이는 무관한 기능 추가였다. 진짜 원인은 **문제의 버전이 콘솔에 등록된 적이 없다**는 것이었다.

★ **되는 놈을 먼저 손에 넣고 나란히 놓을 것.**
정상 동작하는 대조군 없이 세운 가설은 전부 틀렸다. 2연속 오진 후에야 대조표를 만들었고, 표를 만든 순간 답이 나왔다.

---

## 에이전트 보고 규율

두 가지가 반복해서 무너졌다.

**1. 완료분이 보고에서 실종된다** (3회)
→ 보고 첫 블록을 **트랙 상태 표**(해시 / 진행 / 미착수)로 강제한다. "회수 대기"로 뭉개지 않는다.

**2. PR을 draft로 두고 승인 대기해 교착** (3회)
→ 셀프 게이트(CI 체크 + 계약 테스트 + 캡처) 충족 시 자율 즉시 머지.

**3. 검증 없는 "완료"를 쓰지 않는다**
완료를 적을 때 **무엇으로 검증했는지 같이 적는다**. 검증 안 한 건 "실행함"이라고만 적는다.

---

## "데이터가 없다"는 결론에는 자격이 필요하다

**사고 경위**
어떤 지표의 전 세계 데이터를 찾다가 **세 번 파고 "무료로는 존재하지 않는다"고 접었다. 틀렸다.**

| 시도한 축 | 막힌 이유 |
|---|---|
| 상용 평가기관 | 202 봇차단 |
| 위키데이터 정형 속성 | 1,222건인데 **서구 편중** |
| OSM 편집 이력 | **봇 대량편집으로 오염** |
| 위키데이터 창업연도 | 미국 1,011 : 한국 0 |

**"다른 데를 뒤져라"는 재지시를 받고 네 번째에 뚫렸다.** 여행 위키의 큐레이션 데이터가 4/4로 성공했고 아시아 커버리지까지 확보됐다.

**접기 전에 훑을 6축**

1. **공식·심사** — 정부·국제기구·인증기관
2. **오픈데이터** — OSM · Wikidata · Wikivoyage · Socrata · Geofabrik
3. **커뮤니티** — Reddit 아카이브 · HN Algolia · 포럼
4. **현지 플랫폼** — 원어 서비스. ★ **내가 못 읽는다는 게 곧 "데이터가 흩어져 있다"는 뜻은 아니다**
5. **비상업 큐레이션** — 여행 위키 · 공영방송 아카이브
6. **역산 대체** — 직접 신호가 없으면 **'반대의 반대'** 를 찾는다

**판정 규칙**

- ★ **"데이터가 없다"는 위 6축을 전부 찍은 뒤에만 말할 수 있다**
- 접을 땐 **무엇을 어떻게 찍어서 안 됐는지 실측 수치를 남긴다**
- 막힌 이유가 **'접근 차단'이면 대체 경로**를, **'편향'이면 다른 소스**를 찾는다. 두 실패는 처방이 다르다

★ 이건 [「"없음"과 "못 찾음"은 다른 사실이다」](02-python-db.md)의 사람 버전이다. 한 번의 조회가 아니라 **탐색 전체**에 같은 규율이 걸린다 — **"세 번 해봤다"는 소진의 증거가 아니다.**
